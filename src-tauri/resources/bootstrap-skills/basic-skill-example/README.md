# Basic Skill Example

This folder is a minimal Visionox skill example.

- `SKILL.md` describes when the model should use the skill.
- `scripts/` contains optional helper scripts.
- `README.md` explains setup and usage for humans.
